type Tag {
  id: String!
  name: String!
  slug: String!
}

fragment tagFields on Tag {
  id
  name
  slug
}

type Recipe {
  id: String!
  slug: String!
  name: String!
  description: String
  ingredients: [String!]
  instructions: [String!]
  published: Boolean!
  tags: [Tag!]!
}

fragment recipeFields on Recipe {
  id
  name
  slug
  description
  ingredients
  instructions
  published
  tags {
    ...tagFields
  }
}

type RecipeResponse {
  message: String!
  data: Recipe
  error: String
}

fragment recipeResponseFields on RecipeResponse {
  message
  error
  data {
    ...recipeFields
  }
}

type UserPreferences {
  theme: String
}

type PublishOptions {
  publishId: String
  published: Boolean
}

fragment publishOptionsFields on PublishOptions {
  publishId
  published
}

type Me {
  id: String!
  name: String!
  email: String!
  publishid: String!
  publishOptions: PublishOptions
}

query GetRecipes($where: RecipesWhereInput) {
  recipes(where: $where) {
    ...recipeFields
  }
}

query GetTags {
  tags {
    ...tagFields
  }
}

query SearchRecipes($searchQuery: String!) {
  searchRecipes(query: $searchQuery) {
    ...recipeFields
  }
}

query GetMe {
  me {
    id
    name
    email
    publishid
    publishOptions {
      ...publishOptionsFields
    }
  }
}

mutation DeleteRecipe ($recipeSlug: String!) {
  deleteRecipe(slug: $recipeSlug)
}

input RecipesWhereInput {
  slug: String
  tags: [String!]
}

input RecipeUpsertInput {
  authorId: String!
  name: String!
  description: String
  ingredients: [String!]
  instructions: [String!]
}

input PublishOptionsInput {
  publishId: String!
  published: Boolean!
}

mutation UpsertRecipe ($recipeId: String, $recipeData: RecipeUpsertInput!, $tagsConnect: [String!]!, $tagsCreate: [String!]!) {
  upsertRecipe(id: $recipeId, data: $recipeData, tagsConnect: $tagsConnect, tagsCreate: $tagsCreate) {
    ...recipeResponseFields
  }
}

input UpdateUserPreferencesInput {
  theme: String
}

mutation UpdateUserPreferences ($preferences: UpdateUserPreferencesInput) {
  updateUserPreferences(preferences: $preferences) {
    theme
  }
}

mutation DeleteTags ($tagIds: [String!]!) {
  deleteTags(ids: $tagIds)
}

mutation PublishSite ($publishOptions: PublishOptionsInput) {
  publishSite(publishOptions: $publishOptions) {
    publishId
    published
  }
}

type Query {
  recipes(where: RecipesWhereInput): [Recipe!]!
  tags: [Tag!]!
  searchRecipes(query: String): [Recipe!]!
  me: Me!
}

type Mutation {
  deleteRecipe(slug: String!): Boolean
  deleteTags(ids: [String!]!): Boolean
  upsertRecipe(id: String, data: RecipeUpsertInput!, tagsConnect: [String!]!, tagsCreate: [String!]!): RecipeResponse
  updateUserPreferences(preferences: UpdateUserPreferencesInput): UserPreferences
  publishSite(publishOptions: PublishOptionsInput): PublishOptions
}
