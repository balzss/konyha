table:
  schema: public
  name: users
array_relationships:
  - name: recipes
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          schema: public
          name: recipes
select_permissions:
  - role: user
    permission:
      columns:
        - preferences
        - email
        - id
      filter:
        id:
          _eq: X-Hasura-User-Id
