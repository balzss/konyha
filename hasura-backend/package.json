{
  "name": "hasura-backend",
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker-compose up --build",
    "console": "hasura console --console-port 8080 --api-port 8081"
  },
  "devDependencies": {
    "hasura-cli": "^2.5.0"
  }
}
